type Post {
  id: Int
  title: String
}

type User {
  id: Int
  name: String
  posts: [Post] @proxy(get: "http://localhost:5620/users/$id/posts")
}

type Query {
  getUser: User @proxy(get: "/user")
  getUsers: [User] @proxy(get: "/users")
  createUser: User @proxy(post: "/users")
}
